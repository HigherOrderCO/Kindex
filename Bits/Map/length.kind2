Bits.Map.length <a> (map : Bits.Map a) : Nat
Bits.Map.length Bits.Map.new               = Nat.zero
Bits.Map.length (Bits.Map.tie val lft rgt) = Nat.succ (Nat.add (Bits.Map.length lft) (Bits.Map.length rgt))
