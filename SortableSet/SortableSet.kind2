List.range (n: U60) : (List U60)
List.range 0 = List.nil
List.range n = List.cons n (List.range (- n 1))

Main : _
Main =
  let n = 10
  let set = SortableSet.fromList (List.range n)

  (Pair.new
    (SortableSet.elements set)
    (SortableSet.sort set)
  )
