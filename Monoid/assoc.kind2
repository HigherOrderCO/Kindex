Monoid.assoc <t> (monoid: Monoid t) :
  ( let concat = Monoid.concat monoid
    let empty  = Monoid.empty monoid
    ((a: t) -> (b: t) -> (c: t) -> (Equal (concat a (concat b c)) (concat (concat a b) c)))
  )
Monoid.assoc a (Monoid.new b append nil p p1 p2) = p