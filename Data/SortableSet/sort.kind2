// Sorts the elements of the Set
Data.SortableSet.sort (set: Data.SortableSet) : (List U60)
Data.SortableSet.sort (Data.SortableSet.new count segs) = List.foldl List.nil (x => y => (List.merge_sort.merge (a => b => (U60.to_bool (< a b))) x y)) segs
