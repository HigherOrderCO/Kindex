// Merges two ordered lists
SortableSet.merge (xs: (List U60)) (ys: (List U60)) : (List U60)
SortableSet.merge (List.nil t) ys = ys
SortableSet.merge xs (List.nil t) = xs
SortableSet.merge (List.cons t1 x xs) (List.cons t2 y ys) =
  (U60.if (List U60)
    (< x y)
    (List.cons U60 x (SortableSet.merge xs (List.cons t2 y ys)))
    (List.cons U60 y (SortableSet.merge (List.cons t1 x xs) ys))
  )
