use Data.String.Parser as Parser
use Apps.Kind.Parser as KParser
use Apps.Kind.SyntaxTree.Sugared as AST

KParser/pattern : KParser/ Ast/Pattern {
  // TODO: Not very good since it'll try many things that don't apply to patterns
  ask term = KParser/term
  return AST/Term.to_pattern term
}