use Apps.Kind.SyntaxTree.Sugared.ToCore as ToCore
use Apps.Kind.SyntaxTree.Core.Term as Term
use Apps.Kind.SyntaxTree.Range as Range

ToCore/make_u120 (range: Range/) (val: Data.U120) : ToCore/ Term/
ToCore/make_u120 range (Data.U120.new high low) =
  let term = (Term/ctr
    range
    (CtrName/new range "Data.U120.new")
    (Term/num_u60 range high)
    (Term/num_u60 range low)
  )
  ToCore/pure term