// True if a > b, false otherwise
#kdl_name = Nat_gtn
Nat.gtn (a: Nat) (b: Nat) : Bool
Nat.gtn  Nat.zero     b           = Bool.false
Nat.gtn (Nat.succ a)  Nat.zero    = Bool.true
Nat.gtn (Nat.succ a) (Nat.succ b) = Nat.gtn a b