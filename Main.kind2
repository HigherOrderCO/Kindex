use Data as D
use Data.Test as T

Main : T/ {
  T/A
}





//type IsEven ~(n: Nat) {
  //zero : IsEven Nat.zero
  //succ (m: Nat) (p: IsEven m) : IsEven (Nat.succ (Nat.succ m))
//}

//Main (a: Nat) : IsEven (Nat.double a)
//Main Nat.zero = IsEven.zero
//Main (Nat.succ a) = ?hole
