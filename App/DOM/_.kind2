// Automatically derived from App/DOM/_.kind2
#derive[match]
type App.DOM {
  node (tag: String) (props: List (Pair String String)) (style: List (Pair String String)) (children: List App.DOM)
  text (value: String)
  many (children: List App.DOM)
}
